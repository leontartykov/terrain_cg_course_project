#include "driver/invisible/zbuffer.h"
#include "driver/geometry/figure/figure.h"
#include "driver/landscape/landscape.h"

void removing_invisible_lines(ZBuffer &zbuffer, Landscape &landscape)
{
    /*for (int i = 0; i < ; i++){
        for (int j = 0; j < ; j++){
            if (landscape.get_screen_point(i, j).get_z() < zbuffer.get_z_point(i, j)){
                zbuffer.set_z_point(i, j, landscape.get_screen_point(i, j).get_z());
            }
        }
    }*/
}
